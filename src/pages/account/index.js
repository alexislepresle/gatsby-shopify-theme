import React from 'react';
import Layout from "../../components/account/Layout"
const index = () => {
    return(
        <Layout>
            <p>My Account</p>
        </Layout>
    );
};

export default index;